# This config was automatically generated from your source code
# Stacks detected: deps:python:.
version: 2.1
orbs:
  python: circleci/python@2
jobs:
  test-python:
    docker:
      - image: cimg/python:3.8-node  # Use an image with Node.js (needed for Playwright)
    steps:
      - checkout
      - python/install-packages
      - run:
          name: Install Playwright Browsers
          command: |
            npm install -g playwright  # Install Playwright via npm
            playwright install           # Install the necessary browsers
      - run:
          name: Run tests
          command: pytest tests
      - store_test_results:
          path: junit.xml

  deploy:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: deploy
          command: '#e.g. ./deploy.sh'

workflows:
  build-and-test:
    jobs:
      - test-python
      # - deploy:
      #     requires:
      #       - test-python
